---
export interface Props {
    text: string;
    href: string;
    className?: string;
}

const {
    text,
    href,
    className = "",
} = Astro.props;
---

<a
    class:list={["underline", className]}
    href={href}
    target="_blank"
    rel="noopener noreferrer"
>{text}</a>
