export type Site = {
    title: string;
    website: string;
    description: string;
    page_size: number;
};

export type SocialObjects = {
    name: string;
    href: string;
    svgPath: string;
}[];
