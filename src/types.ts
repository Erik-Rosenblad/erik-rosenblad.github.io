export type Site = {
    title: string;
    author: string;
    website: string;
    description: string;
    page_size: number;
};

export type SocialObjects = {
    name: string;
    href: string;
    svgPath: string;
}[];
